#include "main.h"
#include <stdio.h>
/**
 * _strlen - returns the length of a string
 * @s: string
 * Return: length
 */

int _strlen(char *s)
{
	char *str;
	int len;

	while (*str++)
		len++;
	{
		printf("d\n", len);
	}

	return (len);
}
